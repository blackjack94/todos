<li class="<%= "completed" if todo.completed? %>">
	<%= todo.title %>

	<% if todo.completed? %>
		<%= button_to "Mark as Incompleted", todo_completion_path(todo), method: :delete %>
	<% else %>
		<%= button_to "Mark as Completed", todo_completion_path(todo) %>
	<% end %>
</li>